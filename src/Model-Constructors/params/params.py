class Param:
    def __init__(self, init_val, key, description, fixed = True):
        init_value = init_val
        value = init_val
        key = key
        description = description
        fixed = fixed